/*
Calculate A + B.
*/

#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a, b;
    while (scanf("%d %d", &a, &b) > 0) {
        printf("%d\n", a + b);
    }
    return 0;
}

/*
這個題目，根本在在於練習你對輸入輸出的掌握。
其實就是對於你自己所用的語言何以練習ACM的一個方式。
此題全部在於語言。
*/
